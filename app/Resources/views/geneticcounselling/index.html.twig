{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    Genetic counselling list
                </div>
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <div class="row">
                        <div class="col-lg-12">

                            <table class="table table-striped table-bordered table-hover" id="dataTables">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Patientid</th>
                                        <th>Dateofcounselling</th>
                                        <th>Counsellingcomments</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for geneticCounselling in geneticCounsellings %}
                                    <tr>
                                        <td><a href="{{ path('geneticcounselling_show', { 'id': geneticCounselling.id }) }}">{{ geneticCounselling.id }}</a></td>
                                        <td>{{ geneticCounselling.patientId }}</td>
                                        <td>{% if geneticCounselling.dateOfCounselling %}{{ geneticCounselling.dateOfCounselling|date('Y-m-d H:i:s') }}{% endif %}</td>
                                        <td>{{ geneticCounselling.counsellingComments }}</td>
                                        <td>
                                            <ul>
                                                <li>
                                                    <a href="{{ path('geneticcounselling_show', { 'id': geneticCounselling.id }) }}">show</a>
                                                </li>
                                                <li>
                                                    <a href="{{ path('geneticcounselling_edit', { 'id': geneticCounselling.id }) }}">edit</a>
                                                </li>
                                            </ul>
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>

                            <ul>
                                <li>
                                    <a href="{{ path('geneticcounselling_new') }}">Create a new geneticCounselling</a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script>
    $(document).ready(function() {
        $('#dataTables').DataTable({
            responsive: true
        });
    });
    </script>
{% endblock %}